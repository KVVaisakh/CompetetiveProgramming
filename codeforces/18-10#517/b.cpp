#include <bits/stdc++.h>
#define pb push_back
#define pf push_front
#define ll long long
#define ld long double
#define mod 1000000007

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll n,temp;
    vector<ll> a;
    vector<ll> b;
    for(ll i=0;i<n-1;i++)
    {
    	cin>>temp;
    	a.pb(temp);
    }
    for(ll i=0;i<n-1;i++)
    {
    	cin>>temp;
    	b.pb(temp);
    }
    
    return 0;
}