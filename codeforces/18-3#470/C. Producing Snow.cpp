#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long n,t;
    cin>>n;
    vector < long long >v;
    vector < long long >t;
    for(long long i=0;i<n;i++)
    {
        cin>>temp;
        v.push_back(temp);
    }
    for(long long i=0;i<n;i++)
    {
        cin>>temp;
        t.push_back(temp);
    }

    return 0;
}
