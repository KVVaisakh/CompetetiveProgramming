#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t,n,a,b,c,count;
    cin>>t;
    while(t--)
    {
         count=0;
         cin>>n>>a>>b>>c;
         if(n>1)    count+=3;

    }
    return 0;
}
